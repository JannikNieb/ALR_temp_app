# ALR
The project incluced code to visualy express data (temperatures), gained through a measurement, fulfilled by a raspberry pi.
It was made for the aquaponic project of the Atert Lycée Redange.

## Getting started
To install all dependencies run $ pip install -r requirements.txt in a python3 virtualenv To run webserver run python table.py and visit localhost:5000 in your browser